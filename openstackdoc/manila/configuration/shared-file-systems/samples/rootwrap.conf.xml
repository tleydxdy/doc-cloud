<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/manila/doc/source/configuration/shared-file-systems/samples/rootwrap.conf.rst">
    <section ids="rootwrap-conf" names="rootwrap.conf">
        <title>rootwrap.conf</title>
        <paragraph>The <literal>rootwrap.conf</literal> file defines configuration values used by the
            <literal>rootwrap</literal> script when the Shared File Systems service must escalate
            its privileges to those of the root user.</paragraph>
        <literal_block highlight_args="{'linenostart': 1}" language="ini" linenos="False" source="/home/fbaumanis/openstack/soc8_test/openstack_repo/manila/etc/manila/rootwrap.conf" xml:space="preserve"># Configuration for manila-rootwrap
# This file should be owned by (and only-writeable by) the root user

[DEFAULT]
# List of directories to load filter definitions from (separated by ',').
# These directories MUST all be only writeable by root !
filters_path=/etc/manila/rootwrap.d,/usr/share/manila/rootwrap

# List of directories to search executables in, in case filters do not
# explicitly specify a full path (separated by ',')
# If not specified, defaults to system PATH environment variable.
# These directories MUST all be only writeable by root !
exec_dirs=/sbin,/usr/sbin,/bin,/usr/bin,/usr/local/sbin,/usr/local/bin,/usr/lpp/mmfs/bin

# Enable logging to syslog
# Default value is False
use_syslog=False

# Which syslog facility to use.
# Valid values include auth, authpriv, syslog, user0, user1...
# Default value is 'syslog'
syslog_log_facility=syslog

# Which messages to log.
# INFO means log all usage
# ERROR means only log unsuccessful attempts
syslog_log_level=ERROR
</literal_block>
    </section>
</document>
