<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/nova/doc/source/admin/evacuate.rst">
    <section ids="evacuate-instances" names="evacuate\ instances">
        <title>Evacuate instances</title>
        <paragraph>If a hardware malfunction or other error causes a cloud compute node to fail,
            you can evacuate instances to make them available again. You can optionally
            include the target host on the <literal_strong classes="command">nova evacuate</literal_strong> command. If you omit
            the host, the scheduler chooses the target host.</paragraph>
        <paragraph>To preserve user data on the server disk, configure shared storage on the
            target host. When you evacuate the instance, Compute detects whether shared
            storage is available on the target host. Also, you must validate that the
            current VM host is not operational. Otherwise, the evacuation fails.</paragraph>
        <enumerated_list enumtype="arabic" prefix="" suffix=".">
            <list_item>
                <paragraph>To find a host for the evacuated instance, list all hosts:</paragraph>
                <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack host list</literal_block>
            </list_item>
            <list_item>
                <paragraph>Evacuate the instance. You can use the <literal>--password PWD</literal> option to pass the
                    instance password to the command. If you do not specify a password, the
                    command generates and prints one after it finishes successfully. The
                    following command evacuates a server from a failed host to <literal>HOST_B</literal>.</paragraph>
                <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ nova evacuate EVACUATED_SERVER_NAME HOST_B</literal_block>
                <paragraph>The command rebuilds the instance from the original image or volume and
                    returns a password. The command preserves the original configuration, which
                    includes the instance ID, name, uid, IP address, and so on.</paragraph>
                <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">+-----------+--------------+
| Property  |    Value     |
+-----------+--------------+
| adminPass | kRAJpErnT4xZ |
+-----------+--------------+</literal_block>
            </list_item>
            <list_item>
                <paragraph>To preserve the user disk data on the evacuated server, deploy Compute with
                    a shared file system. To configure your system, see
                    <reference internal="True" refuri="configuring-migrations#section-configuring-compute-migrations"><inline classes="std std-ref">Configure live migrations</inline></reference>.  The following example does
                    not change the password.</paragraph>
                <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ nova evacuate EVACUATED_SERVER_NAME HOST_B --on-shared-storage</literal_block>
            </list_item>
        </enumerated_list>
    </section>
</document>
