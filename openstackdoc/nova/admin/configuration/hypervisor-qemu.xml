<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/nova/doc/source/admin/configuration/hypervisor-qemu.rst">
    <target refid="compute-qemu"></target>
    <section ids="qemu compute-qemu" names="qemu compute_qemu">
        <title>QEMU</title>
        <paragraph>From the perspective of the Compute service, the QEMU hypervisor is
            very similar to the KVM hypervisor. Both are controlled through libvirt,
            both support the same feature set, and all virtual machine images that
            are compatible with KVM are also compatible with QEMU.
            The main difference is that QEMU does not support native virtualization.
            Consequently, QEMU has worse performance than KVM and is a poor choice
            for a production deployment.</paragraph>
        <paragraph>The typical uses cases for QEMU are</paragraph>
        <bullet_list bullet="*">
            <list_item>
                <paragraph>Running on older hardware that lacks virtualization support.</paragraph>
            </list_item>
            <list_item>
                <paragraph>Running the Compute service inside of a virtual machine for
                    development or testing purposes, where the hypervisor does not
                    support native virtualization for guests.</paragraph>
            </list_item>
        </bullet_list>
        <paragraph>To enable QEMU, add these settings to <literal>nova.conf</literal>:</paragraph>
        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">compute_driver = libvirt.LibvirtDriver

[libvirt]
virt_type = qemu</literal_block>
        <paragraph>For some operations you may also have to install the
            <literal_strong classes="command">guestmount</literal_strong> utility:</paragraph>
        <paragraph>On Ubuntu:</paragraph>
        <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve"># apt-get install guestmount</literal_block>
        <paragraph>On Red Hat Enterprise Linux, Fedora, or CentOS:</paragraph>
        <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve"># yum install libguestfs-tools</literal_block>
        <paragraph>On openSUSE:</paragraph>
        <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve"># zypper install guestfs-tools</literal_block>
        <paragraph>The QEMU hypervisor supports the following virtual machine image formats:</paragraph>
        <bullet_list bullet="*">
            <list_item>
                <paragraph>Raw</paragraph>
            </list_item>
            <list_item>
                <paragraph>QEMU Copy-on-write (qcow2)</paragraph>
            </list_item>
            <list_item>
                <paragraph>VMware virtual machine disk format (vmdk)</paragraph>
            </list_item>
        </bullet_list>
    </section>
</document>
