<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/nova/doc/source/contributor/testing/serial-console.rst">
    <section ids="testing-serial-console" names="testing\ serial\ console">
        <title>Testing Serial Console</title>
        <paragraph>The main aim of this feature is exposing an interactive web-based
            serial consoles through a web-socket proxy.
            This page describes how to test it from a devstack environment.</paragraph>
        <section ids="setting-up-a-devstack-environment" names="setting\ up\ a\ devstack\ environment">
            <title>Setting up a devstack environment</title>
            <paragraph>For instructions on how to setup devstack with serial console support enabled
                see <reference name="this guide" refuri="http://docs.openstack.org/developer/devstack/guides/nova.html#nova-serialproxy">this guide</reference><target ids="this-guide" names="this\ guide" refuri="http://docs.openstack.org/developer/devstack/guides/nova.html#nova-serialproxy"></target>.</paragraph>
        </section>
        <section ids="testing-the-api" names="testing\ the\ api">
            <title>Testing the API</title>
            <paragraph>Starting a new instance.</paragraph>
            <literal_block highlight_args="{}" language="bash" linenos="False" xml:space="preserve"># cd devstack &amp;&amp; . openrc
# nova boot --flavor 1 --image cirros-0.3.2-x86_64-uec cirros1</literal_block>
            <paragraph>Nova provides a command <title_reference>nova get-serial-console</title_reference> which will returns a
                URL with a valid token to connect to the serial console of VMs.</paragraph>
            <literal_block highlight_args="{}" language="bash" linenos="False" xml:space="preserve"># nova get-serial-console cirros1
+--------+-----------------------------------------------------------------+
| Type   | Url                                                             |
+--------+-----------------------------------------------------------------+
| serial | ws://127.0.0.1:6083/?token=5f7854b7-bf3a-41eb-857a-43fc33f0b1ec |
+--------+-----------------------------------------------------------------+</literal_block>
            <paragraph>Currently nova does not provide any client able to connect from an
                interactive console through a web-socket.
                A simple client for <emphasis>test purpose</emphasis> can be written with few lines of Python.</paragraph>
            <literal_block highlight_args="{}" language="python" linenos="False" xml:space="preserve"># sudo easy_install ws4py || sudo pip install ws4py
# cat &gt;&gt; client.py &lt;&lt;EOF
import sys
from ws4py.client.threadedclient import WebSocketClient
class LazyClient(WebSocketClient):
    def run(self):
        try:
            while not self.terminated:
                try:
                    b = self.sock.recv(4096)
                    sys.stdout.write(b)
                    sys.stdout.flush()
                except: # socket error expected
                    pass
        finally:
            self.terminate()
if __name__ == '__main__':
    if len(sys.argv) != 2 or not sys.argv[1].startswith("ws"):
        print "Usage %s: Please use websocket url"
        print "Example: ws://127.0.0.1:6083/?token=xxx"
        exit(1)
    try:
        ws = LazyClient(sys.argv[1], protocols=['binary'])
        ws.connect()
        while True:
            # keyboard event...
            c = sys.stdin.read(1)
            if c:
                ws.send(c)
        ws.run_forever()
    except KeyboardInterrupt:
        ws.close()
EOF</literal_block>
            <literal_block highlight_args="{}" language="bash" linenos="False" xml:space="preserve"># python client.py ws://127.0.0.1:6083/?token=5f7854b7-bf3a-41eb-857a-43fc33f0b1ec
&lt;enter&gt;
cirros1 login</literal_block>
        </section>
    </section>
</document>
