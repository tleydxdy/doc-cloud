<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.14 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/swift/doc/source/ops_runbook/procedures.rst">
    <section ids="software-configuration-procedures" names="software\ configuration\ procedures">
        <title>Software configuration procedures</title>
        <target refid="fix-broken-gpt-table"></target>
        <section ids="fix-broken-gpt-table-broken-disk-partition fix-broken-gpt-table" names="fix\ broken\ gpt\ table\ (broken\ disk\ partition) fix_broken_gpt_table">
            <title>Fix broken GPT table (broken disk partition)</title>
            <bullet_list bullet="-">
                <list_item>
                    <paragraph>If a GPT table is broken, a message like the following should be
                        observed when the command…</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo parted -l</literal_block>
                </list_item>
                <list_item>
                    <paragraph>… is run.</paragraph>
                    <literal_block classes="code" xml:space="preserve">...
Error: The backup GPT table is corrupt, but the primary appears OK, so that will
be used.
OK/Cancel?</literal_block>
                </list_item>
            </bullet_list>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>To fix this, firstly install the <literal>gdisk</literal> program to fix this:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo aptitude install gdisk</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Run <literal>gdisk</literal> for the particular drive with the damaged partition:</paragraph>
                    <comment xml:space="preserve">code:

$ sudo gdisk /dev/sd*a-l*
GPT fdisk (gdisk) version 0.6.14

Caution: invalid backup GPT header, but valid main header; regenerating
backup header from main header.

Warning! One or more CRCs don't match. You should repair the disk!

Partition table scan:
   MBR: protective
   BSD: not present
   APM: not present
   GPT: damaged
/dev/sd
*****************************************************************************
Caution: Found protective or hybrid MBR and corrupt GPT. Using GPT, but disk
verification and recovery are STRONGLY recommended.
*****************************************************************************</comment>
                </list_item>
                <list_item>
                    <paragraph>On the command prompt, type <literal>r</literal> (recovery and transformation
                        options), followed by <literal>d</literal> (use main GPT header) , <literal>v</literal> (verify disk)
                        and finally <literal>w</literal> (write table to disk and exit). Will also need to
                        enter <literal>Y</literal> when prompted in order to confirm actions.</paragraph>
                    <literal_block classes="code" xml:space="preserve">Command (? for help): r

Recovery/transformation command (? for help): d

Recovery/transformation command (? for help): v

Caution: The CRC for the backup partition table is invalid. This table may
be corrupt. This program will automatically create a new backup partition
table when you save your partitions.

Caution: Partition 1 doesn't begin on a 8-sector boundary. This may
result in degraded performance on some modern (2009 and later) hard disks.

Caution: Partition 2 doesn't begin on a 8-sector boundary. This may
result in degraded performance on some modern (2009 and later) hard disks.

Caution: Partition 3 doesn't begin on a 8-sector boundary. This may
result in degraded performance on some modern (2009 and later) hard disks.

Identified 1 problems!

Recovery/transformation command (? for help): w

Final checks complete. About to write GPT data. THIS WILL OVERWRITE EXISTING
PARTITIONS!!

Do you want to proceed, possibly destroying your data? (Y/N): Y

OK; writing new GUID partition table (GPT).
The operation has completed successfully.</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Running the command:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo parted /dev/sd#</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Should now show that the partition is recovered and healthy again.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Finally, uninstall <literal>gdisk</literal> from the node:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo aptitude remove gdisk</literal_block>
                </list_item>
            </enumerated_list>
            <target refid="fix-broken-xfs-filesystem"></target>
        </section>
        <section ids="procedure-fix-broken-xfs-filesystem fix-broken-xfs-filesystem" names="procedure:\ fix\ broken\ xfs\ filesystem fix_broken_xfs_filesystem">
            <title>Procedure: Fix broken XFS filesystem</title>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>A filesystem may be corrupt or broken if the following output is
                        observed when checking its label:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo xfs_admin -l /dev/sd#
  cache_node_purge: refcount was 1, not zero (node=0x25d5ee0)
  xfs_admin: cannot read root inode (117)
  cache_node_purge: refcount was 1, not zero (node=0x25d92b0)
  xfs_admin: cannot read realtime bitmap inode (117)
  bad sb magic # 0 in AG 1
  failed to read label in AG 1</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Run the following commands to remove the broken/corrupt filesystem and replace.
                        (This example uses the filesystem <literal>/dev/sdb2</literal>) Firstly need to replace the partition:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo parted
GNU Parted 2.3
Using /dev/sda
Welcome to GNU Parted! Type 'help' to view a list of commands.
(parted) select /dev/sdb
Using /dev/sdb
(parted) p
Model: HP LOGICAL VOLUME (scsi)
Disk /dev/sdb: 2000GB
Sector size (logical/physical): 512B/512B
Partition Table: gpt

Number  Start   End     Size    File system  Name   Flags
1      17.4kB  1024MB  1024MB  ext3                 boot
2      1024MB  1751GB  1750GB  xfs          sw-aw2az1-object045-disk1
3      1751GB  2000GB  249GB                        lvm

(parted) rm 2
(parted) mkpart primary 2 -1
Warning: You requested a partition from 2000kB to 2000GB.
The closest location we can manage is 1024MB to 1751GB.
Is this still acceptable to you?
Yes/No? Yes
Warning: The resulting partition is not properly aligned for best performance.
Ignore/Cancel? Ignore
(parted) p
Model: HP LOGICAL VOLUME (scsi)
Disk /dev/sdb: 2000GB
Sector size (logical/physical): 512B/512B
Partition Table: gpt

Number  Start   End     Size    File system  Name     Flags
1      17.4kB  1024MB  1024MB  ext3                  boot
2      1024MB  1751GB  1750GB  xfs          primary
3      1751GB  2000GB  249GB                         lvm

(parted) quit</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Next step is to scrub the filesystem and format:</paragraph>
                    <literal_block classes="code" xml:space="preserve">  $ sudo dd if=/dev/zero of=/dev/sdb2 bs=$((1024*1024)) count=1
  1+0 records in
  1+0 records out
  1048576 bytes (1.0 MB) copied, 0.00480617 s, 218 MB/s
  $ sudo /sbin/mkfs.xfs -f -i size=1024 /dev/sdb2
  meta-data=/dev/sdb2              isize=1024   agcount=4, agsize=106811524 blks
         =                       sectsz=512   attr=2, projid32bit=0
data     =                       bsize=4096   blocks=427246093, imaxpct=5
         =                       sunit=0      swidth=0 blks
naming   =version 2              bsize=4096   ascii-ci=0
log      =internal log           bsize=4096   blocks=208616, version=2
         =                       sectsz=512   sunit=0 blks, lazy-count=1
realtime =none                   extsz=4096   blocks=0, rtextents=0</literal_block>
                </list_item>
                <list_item>
                    <paragraph>You should now label and mount your filesystem.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Can now check to see if the filesystem is mounted using the command:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ mount</literal_block>
                </list_item>
            </enumerated_list>
            <target refid="checking-if-account-ok"></target>
        </section>
        <section ids="procedure-checking-if-an-account-is-okay checking-if-account-ok" names="procedure:\ checking\ if\ an\ account\ is\ okay checking_if_account_ok">
            <title>Procedure: Checking if an account is okay</title>
            <note>
                <paragraph><literal>swift-direct</literal> is only available in the HPE Helion Public Cloud.
                    Use <literal>swiftly</literal> as an alternate (or use <literal>swift-get-nodes</literal> as explained
                    here).</paragraph>
            </note>
            <paragraph>You must know the tenant/project ID. You can check if the account is okay as follows from a proxy.</paragraph>
            <literal_block classes="code" xml:space="preserve">$ sudo -u swift  /opt/hp/swift/bin/swift-direct show AUTH_&lt;project-id&gt;</literal_block>
            <paragraph>The response will either be similar to a swift list of the account
                containers, or an error indicating that the resource could not be found.</paragraph>
            <paragraph>Alternatively, you can use <literal>swift-get-nodes</literal> to find the account database
                files. Run the following on a proxy:</paragraph>
            <literal_block classes="code" xml:space="preserve">$ sudo swift-get-nodes /etc/swift/account.ring.gz  AUTH_&lt;project-id&gt;</literal_block>
            <paragraph>The response will print curl/ssh commands that will list the replicated
                account databases. Use the indicated <literal>curl</literal> or <literal>ssh</literal> commands to check
                the status and existence of the account.</paragraph>
        </section>
        <section ids="procedure-getting-swift-account-stats" names="procedure:\ getting\ swift\ account\ stats">
            <title>Procedure: Getting  swift account stats</title>
            <note>
                <paragraph><literal>swift-direct</literal> is specific to the HPE Helion Public Cloud. Go look at
                    <literal>swifty</literal> for an alternate or use <literal>swift-get-nodes</literal> as explained
                    in <reference internal="True" refid="checking-if-account-ok"><inline classes="std std-ref">Procedure: Checking if an account is okay</inline></reference>.</paragraph>
            </note>
            <paragraph>This procedure describes how you determine the swift usage for a given
                swift account, that is the number of containers, number of objects and
                total bytes used. To do this you will need the project ID.</paragraph>
            <paragraph>Log onto one of the swift proxy servers.</paragraph>
            <paragraph>Use swift-direct to show this accounts usage:</paragraph>
            <literal_block classes="code" xml:space="preserve">$ sudo -u swift /opt/hp/swift/bin/swift-direct show AUTH_&lt;project-id&gt;
Status: 200
      Content-Length: 0
      Accept-Ranges: bytes
      X-Timestamp: 1379698586.88364
      X-Account-Bytes-Used: 67440225625994
      X-Account-Container-Count: 1
      Content-Type: text/plain; charset=utf-8
      X-Account-Object-Count: 8436776
      Status: 200
      name: my_container  count: 8436776  bytes: 67440225625994</literal_block>
            <paragraph>This account has 1 container. That container has 8436776 objects. The
                total bytes used is 67440225625994.</paragraph>
        </section>
        <section ids="procedure-revive-a-deleted-account" names="procedure:\ revive\ a\ deleted\ account">
            <title>Procedure: Revive a deleted account</title>
            <paragraph>Swift accounts are normally not recreated. If a tenant/project is deleted,
                the account can then be deleted. If the user wishes to use Swift again,
                the normal process is to create a new tenant/project – and hence a
                new Swift account.</paragraph>
            <paragraph>However, if the Swift account is deleted, but the tenant/project is not
                deleted from Keystone, the user can no longer access the account. This
                is because the account is marked deleted in Swift. You can revive
                the account as described in this process.</paragraph>
            <note>
                <paragraph>The containers and objects in the “old” account cannot be listed
                    anymore. In addition, if the Account Reaper process has not
                    finished reaping the containers and objects in the “old” account, these
                    are effectively orphaned and it is virtually impossible to find and delete
                    them to free up disk space.</paragraph>
            </note>
            <paragraph>The solution is to delete the account database files and
                re-create the account as follows:</paragraph>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>You must know the tenant/project ID. The account name is AUTH_&lt;project-id&gt;.
                        In this example, the tenant/project is <literal>4ebe3039674d4864a11fe0864ae4d905</literal>
                        so the Swift account name is <literal>AUTH_4ebe3039674d4864a11fe0864ae4d905</literal>.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Use <literal>swift-get-nodes</literal> to locate the account’s database files (on three
                        servers). The output has been truncated so we can focus on the import pieces
                        of data:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ sudo swift-get-nodes /etc/swift/account.ring.gz AUTH_4ebe3039674d4864a11fe0864ae4d905
...
curl -I -XHEAD "http://192.168.245.5:6202/disk1/3934/AUTH_4ebe3039674d4864a11fe0864ae4d905"
curl -I -XHEAD "http://192.168.245.3:6202/disk0/3934/AUTH_4ebe3039674d4864a11fe0864ae4d905"
curl -I -XHEAD "http://192.168.245.4:6202/disk1/3934/AUTH_4ebe3039674d4864a11fe0864ae4d905"
...
Use your own device location of servers:
such as "export DEVICE=/srv/node"
ssh 192.168.245.5 "ls -lah ${DEVICE:-/srv/node*}/disk1/accounts/3934/052/f5ecf8b40de3e1b0adb0dbe576874052"
ssh 192.168.245.3 "ls -lah ${DEVICE:-/srv/node*}/disk0/accounts/3934/052/f5ecf8b40de3e1b0adb0dbe576874052"
ssh 192.168.245.4 "ls -lah ${DEVICE:-/srv/node*}/disk1/accounts/3934/052/f5ecf8b40de3e1b0adb0dbe576874052"
...
note: `/srv/node*` is used as default value of `devices`, the real value is set in the config file on each storage node.</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Before proceeding check that the account is really deleted by using curl. Execute the
                        commands printed by <literal>swift-get-nodes</literal>. For example:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ curl -I -XHEAD "http://192.168.245.5:6202/disk1/3934/AUTH_4ebe3039674d4864a11fe0864ae4d905"
HTTP/1.1 404 Not Found
Content-Length: 0
Content-Type: text/html; charset=utf-8</literal_block>
                    <paragraph>Repeat for the other two servers (192.168.245.3 and 192.168.245.4).
                        A <literal>404 Not Found</literal> indicates that the account is deleted (or never existed).</paragraph>
                    <paragraph>If you get a <literal>204 No Content</literal> response, do <strong>not</strong> proceed.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Use the ssh commands printed by <literal>swift-get-nodes</literal> to check if database
                        files exist. For example:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$  ssh 192.168.245.5 "ls -lah ${DEVICE:-/srv/node*}/disk1/accounts/3934/052/f5ecf8b40de3e1b0adb0dbe576874052"
total 20K
drwxr-xr-x 2 swift swift 110 Mar  9 10:22 .
drwxr-xr-x 3 swift swift  45 Mar  9 10:18 ..
-rw------- 1 swift swift 17K Mar  9 10:22 f5ecf8b40de3e1b0adb0dbe576874052.db
-rw-r--r-- 1 swift swift   0 Mar  9 10:22 f5ecf8b40de3e1b0adb0dbe576874052.db.pending
-rwxr-xr-x 1 swift swift   0 Mar  9 10:18 .lock</literal_block>
                    <paragraph>Repeat for the other two servers (192.168.245.3 and 192.168.245.4).</paragraph>
                    <paragraph>If no files exist, no further action is needed.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Stop Swift processes on all nodes listed by <literal>swift-get-nodes</literal>
                        (In this example, that is 192.168.245.3, 192.168.245.4 and 192.168.245.5).</paragraph>
                </list_item>
                <list_item>
                    <paragraph>We recommend you make backup copies of the database files.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Delete the database files. For example:</paragraph>
                    <literal_block classes="code" xml:space="preserve">$ ssh 192.168.245.5
$ cd /srv/node/disk1/accounts/3934/052/f5ecf8b40de3e1b0adb0dbe576874052
$ sudo rm *</literal_block>
                    <paragraph>Repeat for the other two servers (192.168.245.3 and 192.168.245.4).</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Restart Swift on all three servers</paragraph>
                </list_item>
            </enumerated_list>
            <paragraph>At this stage, the account is fully deleted. If you enable the auto-create option, the
                next time the user attempts to access the account, the account will be created.
                You may also use swiftly to recreate the account.</paragraph>
        </section>
        <section ids="procedure-temporarily-stop-load-balancers-from-directing-traffic-to-a-proxy-server" names="procedure:\ temporarily\ stop\ load\ balancers\ from\ directing\ traffic\ to\ a\ proxy\ server">
            <title>Procedure: Temporarily stop load balancers from directing traffic to a proxy server</title>
            <paragraph>You can stop the load balancers sending requests to a proxy server as
                follows. This can be useful when a proxy is misbehaving but you need
                Swift running to help diagnose the problem. By removing from the load
                balancers, customer’s are not impacted by the misbehaving proxy.</paragraph>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>Ensure that in /etc/swift/proxy-server.conf the <literal>disable_path</literal> variable is set to
                        <literal>/etc/swift/disabled-by-file</literal>.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Log onto the proxy node.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Shut down Swift as follows:</paragraph>
                    <literal_block classes="code" xml:space="preserve">sudo swift-init proxy shutdown</literal_block>
                    <note>
                        <paragraph>Shutdown, not stop.</paragraph>
                    </note>
                </list_item>
                <list_item>
                    <paragraph>Create the <literal>/etc/swift/disabled-by-file</literal> file. For example:</paragraph>
                    <literal_block classes="code" xml:space="preserve">sudo touch /etc/swift/disabled-by-file</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Optional, restart Swift:</paragraph>
                    <literal_block classes="code" xml:space="preserve">sudo swift-init proxy start</literal_block>
                </list_item>
            </enumerated_list>
            <paragraph>It works because the healthcheck middleware looks for /etc/swift/disabled-by-file.
                If it exists, the middleware will return 503/error instead of 200/OK. This means the load balancer
                should stop sending traffic to the proxy.</paragraph>
        </section>
        <section ids="procedure-ad-hoc-disk-performance-test" names="procedure:\ ad-hoc\ disk\ performance\ test">
            <title>Procedure: Ad-Hoc disk performance test</title>
            <paragraph>You can get an idea whether a disk drive is performing as follows:</paragraph>
            <literal_block classes="code" xml:space="preserve">sudo dd bs=1M count=256 if=/dev/zero conv=fdatasync of=/srv/node/disk11/remember-to-delete-this-later</literal_block>
            <paragraph>You can expect ~600MB/sec. If you get a low number, repeat many times as
                Swift itself may also read or write to the disk, hence giving a lower
                number.</paragraph>
        </section>
    </section>
</document>
