<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/horizon/doc/source/contributor/tutorials/dashboard.rst">
    <target refid="tutorials-dashboard"></target>
    <section ids="tutorial-building-a-dashboard-using-horizon tutorials-dashboard" names="tutorial:\ building\ a\ dashboard\ using\ horizon tutorials-dashboard">
        <title>Tutorial: Building a Dashboard using Horizon</title>
        <paragraph>This tutorial covers how to use the various components in horizon to build
            an example dashboard and a panel with a tab which has a table containing data
            from the back end.</paragraph>
        <paragraph>As an example, we’ll create a new <literal>My Dashboard</literal> dashboard with a <literal>My Panel</literal>
            panel that has an <literal>Instances Tab</literal> tab. The tab has a table which contains the
            data pulled by the Nova instances API.</paragraph>
        <note>
            <paragraph>This tutorial assumes you have either a <literal>devstack</literal> or
                <literal>openstack</literal> environment up and running.  There are a variety of
                other resources which may be helpful to read first.  For example,
                you may want to start with the <reference internal="True" refuri="../quickstart#quickstart"><inline classes="std std-ref">Quickstart</inline></reference> or the <reference name="Django tutorial" refuri="https://docs.djangoproject.com/en/dev/intro/tutorial01/">Django
                    tutorial</reference>.</paragraph>
            <target ids="django-tutorial" names="django\ tutorial" refuri="https://docs.djangoproject.com/en/dev/intro/tutorial01/"></target>
        </note>
        <section ids="creating-a-dashboard" names="creating\ a\ dashboard">
            <title>Creating a dashboard</title>
            <section ids="the-quick-version" names="the\ quick\ version">
                <title>The quick version</title>
                <paragraph>Horizon provides a custom management command to create a typical base
                    dashboard structure for you. Run the following commands in your Horizon root
                    directory. It generates most of the boilerplate code you need:</paragraph>
                <literal_block xml:space="preserve">$ mkdir openstack_dashboard/dashboards/mydashboard

$ tox -e manage -- startdash mydashboard \
  --target openstack_dashboard/dashboards/mydashboard

$ mkdir openstack_dashboard/dashboards/mydashboard/mypanel

$ tox -e manage -- startpanel mypanel \
  --dashboard=openstack_dashboard.dashboards.mydashboard \
  --target=openstack_dashboard/dashboards/mydashboard/mypanel</literal_block>
                <paragraph>You will notice that the directory <literal>mydashboard</literal> gets automatically
                    populated with the files related to a dashboard and the <literal>mypanel</literal> directory
                    gets automatically populated with the files related to a panel.</paragraph>
            </section>
            <section dupnames="structure" ids="structure">
                <title>Structure</title>
                <paragraph>If you use the <literal>tree mydashboard</literal> command to list the <literal>mydashboard</literal>
                    directory in <literal>openstack_dashboard/dashboards</literal> , you will see a directory
                    structure that looks like the following:</paragraph>
                <literal_block xml:space="preserve">mydashboard
├── dashboard.py
├── dashboard.pyc
├── __init__.py
├── __init__.pyc
├── mypanel
│   ├── __init__.py
│   ├── panel.py
│   ├── templates
│   │   └── mypanel
│   │       └── index.html
│   ├── tests.py
│   ├── urls.py
│   └── views.py
├── static
│   └── mydashboard
│       ├── css
│       │   └── mydashboard.css
│       └── js
│           └── mydashboard.js
└── templates
    └── mydashboard
        └── base.html</literal_block>
                <paragraph>For this tutorial, we will not deal with the static directory, or the
                    <literal>tests.py</literal> file. Leave them as they are.</paragraph>
                <paragraph>With the rest of the files and directories in place, we can move on to add our
                    own dashboard.</paragraph>
            </section>
            <section ids="defining-a-dashboard" names="defining\ a\ dashboard">
                <title>Defining a dashboard</title>
                <paragraph>Open the <literal>dashboard.py</literal> file. You will notice the following code has been
                    automatically generated:</paragraph>
                <literal_block xml:space="preserve">from django.utils.translation import ugettext_lazy as _

import horizon


class Mydashboard(horizon.Dashboard):
   name = _("Mydashboard")
   slug = "mydashboard"
   panels = ()           # Add your panels here.
   default_panel = ''    # Specify the slug of the dashboard's default panel.


horizon.register(Mydashboard)</literal_block>
                <paragraph>If you want the dashboard name to be something else, you can change the <literal>name</literal>
                    attribute in the <literal>dashboard.py</literal> file . For example, you can change it
                    to be <literal>My Dashboard</literal></paragraph>
                <literal_block xml:space="preserve">name = _("My Dashboard")</literal_block>
                <paragraph>A dashboard class will usually contain a <literal>name</literal> attribute (the display name of
                    the dashboard), a <literal>slug</literal> attribute (the internal name that could be referenced
                    by other components), a list of panels, default panel, etc. We will cover how
                    to add a panel in the next section.</paragraph>
            </section>
        </section>
        <section ids="creating-a-panel" names="creating\ a\ panel">
            <title>Creating a panel</title>
            <paragraph>We’ll create a panel and call it <literal>My Panel</literal>.</paragraph>
            <section dupnames="structure" ids="id1">
                <title>Structure</title>
                <paragraph>As described above, the <literal>mypanel</literal> directory under
                    <literal>openstack_dashboard/dashboards/mydashboard</literal> should look like the following:</paragraph>
                <literal_block xml:space="preserve">mypanel
 ├── __init__.py
 ├── models.py
 ├── panel.py
 ├── templates
 │   └── mypanel
 │     └── index.html
 ├── tests.py
 ├── urls.py
 └── views.py</literal_block>
            </section>
            <section ids="defining-a-panel" names="defining\ a\ panel">
                <title>Defining a panel</title>
                <paragraph>The <literal>panel.py</literal> file referenced above has a special meaning.
                    Within a dashboard, any module name listed in the <literal>panels</literal> attribute on the
                    dashboard class will be auto-discovered by looking for the <literal>panel.py</literal> file in
                    a corresponding directory (the details are a bit magical, but have been
                    thoroughly vetted in Django’s admin codebase).</paragraph>
                <paragraph>Open the <literal>panel.py</literal> file, you will have the following auto-generated code:</paragraph>
                <literal_block xml:space="preserve">from django.utils.translation import ugettext_lazy as _

import horizon

from openstack_dashboard.dashboards.mydashboard import dashboard


class Mypanel(horizon.Panel):
    name = _("Mypanel")
    slug = "mypanel"


dashboard.Mydashboard.register(Mypanel)</literal_block>
                <paragraph>If you want the panel name to be something else, you can change the <literal>name</literal>
                    attribute in the <literal>panel.py</literal> file . For example, you can change it to be
                    <literal>My Panel</literal>:</paragraph>
                <literal_block xml:space="preserve">name = _("My Panel")</literal_block>
                <paragraph>Open the <literal>dashboard.py</literal> file again, insert the following code above the
                    <literal>Mydashboard</literal> class. This code defines the <literal>Mygroup</literal> class and adds a panel
                    called <literal>mypanel</literal>:</paragraph>
                <literal_block xml:space="preserve">class Mygroup(horizon.PanelGroup):
    slug = "mygroup"
    name = _("My Group")
    panels = ('mypanel',)</literal_block>
                <paragraph>Modify the <literal>Mydashboard</literal> class to include <literal>Mygroup</literal> and add <literal>mypanel</literal> as
                    the default panel:</paragraph>
                <literal_block xml:space="preserve">class Mydashboard(horizon.Dashboard):
   name = _("My Dashboard")
   slug = "mydashboard"
   panels = (Mygroup,)  # Add your panels here.
   default_panel = 'mypanel'  # Specify the slug of the default panel.</literal_block>
                <paragraph>The completed <literal>dashboard.py</literal> file should look like
                    the following:</paragraph>
                <literal_block xml:space="preserve">from django.utils.translation import ugettext_lazy as _

import horizon


class Mygroup(horizon.PanelGroup):
    slug = "mygroup"
    name = _("My Group")
    panels = ('mypanel',)


class Mydashboard(horizon.Dashboard):
    name = _("My Dashboard")
    slug = "mydashboard"
    panels = (Mygroup,)  # Add your panels here.
    default_panel = 'mypanel'  # Specify the slug of the default panel.


horizon.register(Mydashboard)</literal_block>
            </section>
            <section ids="tables-tabs-and-views" names="tables,\ tabs,\ and\ views">
                <title>Tables, Tabs, and Views</title>
                <paragraph>We’ll start with the table, combine that with the tabs, and then build our
                    view from the pieces.</paragraph>
                <section ids="defining-a-table" names="defining\ a\ table">
                    <title>Defining a table</title>
                    <paragraph>Horizon provides a <literal classes="xref py py-class">SelfHandlingForm</literal>
                        <reference internal="True" reftitle="horizon.tables.DataTable" refuri="../ref/tables#horizon.tables.DataTable"><literal classes="xref py py-class">DataTable</literal></reference> class which simplifies the vast majority of
                        displaying data to an end-user. We’re just going to skim the surface here, but
                        it has a tremendous number of capabilities. Create a <literal>tables.py</literal> file under
                        the <literal>mypanel</literal> directory and add the following code:</paragraph>
                    <literal_block xml:space="preserve">from django.utils.translation import ugettext_lazy as _

from horizon import tables


class InstancesTable(tables.DataTable):
    name = tables.Column("name", verbose_name=_("Name"))
    status = tables.Column("status", verbose_name=_("Status"))
    zone = tables.Column('availability_zone',
                          verbose_name=_("Availability Zone"))
    image_name = tables.Column('image_name', verbose_name=_("Image Name"))

    class Meta(object):
        name = "instances"
        verbose_name = _("Instances")</literal_block>
                    <paragraph>There are several things going on here… we created a table subclass,
                        and defined four columns that we want to retrieve data and display.
                        Each of those columns defines what attribute it accesses on the instance object
                        as the first argument, and since we like to make everything translatable,
                        we give each column a <literal>verbose_name</literal> that’s marked for translation.</paragraph>
                    <paragraph>Lastly, we added a <literal>Meta</literal> class which indicates the meta object that describes
                        the <literal>instances</literal> table.</paragraph>
                    <note>
                        <paragraph>This is a slight simplification from the reality of how the instance
                            object is actually structured. In reality, accessing other attributes
                            requires an additional step.</paragraph>
                    </note>
                </section>
                <section ids="adding-actions-to-a-table" names="adding\ actions\ to\ a\ table">
                    <title>Adding actions to a table</title>
                    <paragraph>Horizon provides three types of basic action classes which can be taken
                        on a table’s data:</paragraph>
                    <bullet_list bullet="-">
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.Action" refuri="../ref/tables#horizon.tables.Action"><literal classes="xref py py-class">Action</literal></reference></paragraph>
                        </list_item>
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.LinkAction" refuri="../ref/tables#horizon.tables.LinkAction"><literal classes="xref py py-class">LinkAction</literal></reference></paragraph>
                        </list_item>
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.FilterAction" refuri="../ref/tables#horizon.tables.FilterAction"><literal classes="xref py py-class">FilterAction</literal></reference></paragraph>
                        </list_item>
                    </bullet_list>
                    <paragraph>There are also additional actions which are extensions of the basic Action
                        classes:</paragraph>
                    <bullet_list bullet="-">
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.BatchAction" refuri="../ref/tables#horizon.tables.BatchAction"><literal classes="xref py py-class">BatchAction</literal></reference></paragraph>
                        </list_item>
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.DeleteAction" refuri="../ref/tables#horizon.tables.DeleteAction"><literal classes="xref py py-class">DeleteAction</literal></reference></paragraph>
                        </list_item>
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.UpdateAction" refuri="../ref/tables#horizon.tables.UpdateAction"><literal classes="xref py py-class">UpdateAction</literal></reference> <strong>DEPRECATED</strong></paragraph>
                        </list_item>
                        <list_item>
                            <paragraph><reference internal="True" reftitle="horizon.tables.FixedFilterAction" refuri="../ref/tables#horizon.tables.FixedFilterAction"><literal classes="xref py py-class">FixedFilterAction</literal></reference></paragraph>
                        </list_item>
                    </bullet_list>
                    <paragraph>Now let’s create and add a filter action to the table. To do so, we will need
                        to edit the <literal>tables.py</literal> file used above. To add a filter action which will
                        only show rows which contain the string entered in the filter field, we
                        must first define the action:</paragraph>
                    <literal_block xml:space="preserve">class MyFilterAction(tables.FilterAction):
    name = "myfilter"</literal_block>
                    <note>
                        <paragraph>The action specified above will default the <literal>filter_type</literal> to be <literal>"query"</literal>.
                            This means that the filter will use the client side table sorter.</paragraph>
                    </note>
                    <paragraph>Then, we add that action to the table actions for our table.:</paragraph>
                    <literal_block xml:space="preserve">class InstancesTable:
    class Meta(object):
        table_actions = (MyFilterAction,)</literal_block>
                    <paragraph>The completed <literal>tables.py</literal> file should look like the following:</paragraph>
                    <literal_block xml:space="preserve">from django.utils.translation import ugettext_lazy as _

from horizon import tables


class MyFilterAction(tables.FilterAction):
    name = "myfilter"


class InstancesTable(tables.DataTable):
    name = tables.Column('name', \
                         verbose_name=_("Name"))
    status = tables.Column('status', \
                           verbose_name=_("Status"))
    zone = tables.Column('availability_zone', \
                         verbose_name=_("Availability Zone"))
    image_name = tables.Column('image_name', \
                               verbose_name=_("Image Name"))

    class Meta(object):
        name = "instances"
        verbose_name = _("Instances")
        table_actions = (MyFilterAction,)</literal_block>
                </section>
                <section ids="defining-tabs" names="defining\ tabs">
                    <title>Defining tabs</title>
                    <paragraph>So we have a table, ready to receive our data. We could go straight to a view
                        from here, but in this case we’re also going to use horizon’s
                        <reference internal="True" reftitle="horizon.tabs.TabGroup" refuri="../ref/tabs#horizon.tabs.TabGroup"><literal classes="xref py py-class">TabGroup</literal></reference> class.</paragraph>
                    <paragraph>Create a <literal>tabs.py</literal> file under the <literal>mypanel</literal> directory. Let’s make a tab
                        group which has one tab. The completed code should look like the following:</paragraph>
                    <literal_block xml:space="preserve">from django.utils.translation import ugettext_lazy as _

from horizon import exceptions
from horizon import tabs

from openstack_dashboard import api
from openstack_dashboard.dashboards.mydashboard.mypanel import tables


class InstanceTab(tabs.TableTab):
    name = _("Instances Tab")
    slug = "instances_tab"
    table_classes = (tables.InstancesTable,)
    template_name = ("horizon/common/_detail_table.html")
    preload = False

    def has_more_data(self, table):
        return self._has_more

    def get_instances_data(self):
        try:
            marker = self.request.GET.get(
                        tables.InstancesTable._meta.pagination_param, None)

            instances, self._has_more = api.nova.server_list(
                self.request,
                search_opts={'marker': marker, 'paginate': True})

            return instances
        except Exception:
            self._has_more = False
            error_message = _('Unable to get instances')
            exceptions.handle(self.request, error_message)

            return []

class MypanelTabs(tabs.TabGroup):
    slug = "mypanel_tabs"
    tabs = (InstanceTab,)
    sticky = True</literal_block>
                    <paragraph>This tab gets a little more complicated. The tab handles data tables (and
                        all their associated features), and it also uses the <literal>preload</literal> attribute to
                        specify that this tab shouldn’t be loaded by default. It will instead be loaded
                        via AJAX when someone clicks on it, saving us on API calls in the vast majority
                        of cases.</paragraph>
                    <paragraph>Additionally, the displaying of the table is handled by a reusable template,
                        <literal>horizon/common/_detail_table.html</literal>. Some simple pagination code was added
                        to handle large instance lists.</paragraph>
                    <paragraph>Lastly, this code introduces the concept of error handling in horizon.
                        The <reference internal="True" reftitle="horizon.exceptions.handle" refuri="../ref/exceptions#horizon.exceptions.handle"><literal classes="xref py py-func">horizon.exceptions.handle()</literal></reference> function is a centralized error
                        handling mechanism that takes all the guess-work and inconsistency out of
                        dealing with exceptions from the API. Use it everywhere.</paragraph>
                </section>
                <section ids="tying-it-together-in-a-view" names="tying\ it\ together\ in\ a\ view">
                    <title>Tying it together in a view</title>
                    <paragraph>There are lots of pre-built class-based views in horizon. We try to provide
                        the starting points for all the common combinations of components.</paragraph>
                    <paragraph>Open the <literal>views.py</literal> file, the auto-generated code is like the following:</paragraph>
                    <literal_block xml:space="preserve">from horizon import views


class IndexView(views.APIView):
    # A very simple class-based view...
    template_name = 'mydashboard/mypanel/index.html'

    def get_data(self, request, context, *args, **kwargs):
        # Add data to the context here...
        return context</literal_block>
                    <paragraph>In this case we want a starting view type that works with both tabs and
                        tables… that’d be the <reference internal="True" reftitle="horizon.tabs.TabbedTableView" refuri="../ref/tabs#horizon.tabs.TabbedTableView"><literal classes="xref py py-class">TabbedTableView</literal></reference> class. It takes
                        the best of the dynamic delayed-loading capabilities tab groups provide and
                        mixes in the actions and AJAX-updating that tables are capable of with almost
                        no work on the user’s end. Change <literal>views.APIView</literal> to be
                        <literal>tabs.TabbedTableView</literal> and add <literal>MypanelTabs</literal> as the tab group class in the
                        <literal>IndexView</literal> class:</paragraph>
                    <literal_block xml:space="preserve">class IndexView(tabs.TabbedTableView):
    tab_group_class = mydashboard_tabs.MypanelTabs</literal_block>
                    <paragraph>After importing the proper package, the completed <literal>views.py</literal> file  now looks
                        like the following:</paragraph>
                    <literal_block xml:space="preserve">from horizon import tabs

from openstack_dashboard.dashboards.mydashboard.mypanel \
    import tabs as mydashboard_tabs


class IndexView(tabs.TabbedTableView):
    tab_group_class = mydashboard_tabs.MypanelTabs
    template_name = 'mydashboard/mypanel/index.html'

    def get_data(self, request, context, *args, **kwargs):
        # Add data to the context here...
        return context</literal_block>
                </section>
            </section>
            <section ids="urls" names="urls">
                <title>URLs</title>
                <paragraph>The auto-generated <literal>urls.py</literal> file is like:</paragraph>
                <literal_block xml:space="preserve">from django.conf.urls import url

from openstack_dashboard.dashboards.mydashboard.mypanel import views


urlpatterns = [
    url(r'^$', views.IndexView.as_view(), name='index'),
]</literal_block>
                <section ids="the-template" names="the\ template">
                    <title>The template</title>
                    <paragraph>Open the <literal>index.html</literal> file in the <literal>mydashboard/mypanel/templates/mypanel</literal>
                        directory, the auto-generated code is like the following:</paragraph>
                    <literal_block xml:space="preserve">{% extends 'base.html' %}
{% load i18n %}
{% block title %}{% trans "Mypanel" %}{% endblock %}

{% block page_header %}
    {% include "horizon/common/_page_header.html" with title=_("Mypanel") %}
{% endblock page_header %}

{% block main %}
{% endblock %}</literal_block>
                    <paragraph>The <literal>main</literal> block must be modified to insert the following code:</paragraph>
                    <literal_block xml:space="preserve">&lt;div class="row"&gt;
   &lt;div class="col-sm-12"&gt;
   {{ tab_group.render }}
   &lt;/div&gt;
&lt;/div&gt;</literal_block>
                    <paragraph>If you want to change the title of the <literal>index.html</literal> file to be something else,
                        you can change it. For example, change it to be <literal>My Panel</literal> in the
                        <literal>block title</literal> section.  If you want the <literal>title</literal> in the <literal>block page_header</literal>
                        section to be something else, you can change it. For example, change it to be
                        <literal>My Panel</literal>. The updated code could be like:</paragraph>
                    <literal_block xml:space="preserve">{% extends 'base.html' %}
{% load i18n %}
{% block title %}{% trans "My Panel" %}{% endblock %}

{% block page_header %}
   {% include "horizon/common/_page_header.html" with title=_("My Panel") %}
{% endblock page_header %}

{% block main %}
&lt;div class="row"&gt;
   &lt;div class="col-sm-12"&gt;
   {{ tab_group.render }}
   &lt;/div&gt;
&lt;/div&gt;
{% endblock %}</literal_block>
                    <paragraph>This gives us a custom page title, a header, and renders our tab group provided
                        by the view.</paragraph>
                    <paragraph>With all our code in place, the only thing left to do is to integrate it into
                        our OpenStack Dashboard site.</paragraph>
                    <note>
                        <paragraph>For more information about Django views, URLs and templates, please refer
                            to the <reference name="Django documentation" refuri="https://docs.djangoproject.com/en/dev/">Django documentation</reference>.</paragraph>
                        <target ids="django-documentation" names="django\ documentation" refuri="https://docs.djangoproject.com/en/dev/"></target>
                    </note>
                </section>
            </section>
        </section>
        <section ids="enable-and-show-the-dashboard" names="enable\ and\ show\ the\ dashboard">
            <title>Enable and show the dashboard</title>
            <paragraph>In order to make <literal>My Dashboard</literal> show up along with the existing dashboards
                like <literal>Project</literal> or <literal>Admin</literal> on horizon, you need to create a file called
                <literal>_50_mydashboard.py</literal> under <literal>openstack_dashboard/enabled</literal> and add the
                following:</paragraph>
            <literal_block xml:space="preserve"># The name of the dashboard to be added to HORIZON['dashboards']. Required.
DASHBOARD = 'mydashboard'

# If set to True, this dashboard will not be added to the settings.
DISABLED = False

# A list of applications to be added to INSTALLED_APPS.
ADD_INSTALLED_APPS = [
    'openstack_dashboard.dashboards.mydashboard',
]</literal_block>
        </section>
        <section ids="run-and-check-the-dashboard" names="run\ and\ check\ the\ dashboard">
            <title>Run and check the dashboard</title>
            <paragraph>Everything is in place, now run <literal>Horizon</literal> on the different port:</paragraph>
            <literal_block xml:space="preserve">$ tox -e runserver -- 0:9000</literal_block>
            <paragraph>Go to <literal>http://&lt;your server&gt;:9000</literal> using a browser. After login as an admin
                you should be able see <literal>My Dashboard</literal> shows up at the left side on horizon.
                Click it, <literal>My Group</literal> will expand with <literal>My Panel</literal>. Click on <literal>My Panel</literal>,
                the right side panel will display an <literal>Instances Tab</literal> which has an
                <literal>Instances</literal> table.</paragraph>
            <paragraph>If you don’t see any instance data, you haven’t created any instances yet. Go to
                dashboard <literal>Project</literal> -&gt; <literal>Images</literal>, select a small image, for example,
                <literal>cirros-0.3.1-x86_64-uec</literal> , click <literal>Launch</literal> and enter an <literal>Instance Name</literal>,
                click the button <literal>Launch</literal>. It should create an instance if the OpenStack or
                devstack is correctly set up. Once the creation of an instance is successful, go
                to <literal>My Dashboard</literal> again to check the data.</paragraph>
        </section>
        <section ids="adding-a-complex-action-to-a-table" names="adding\ a\ complex\ action\ to\ a\ table">
            <title>Adding a complex action to a table</title>
            <paragraph>For a more detailed look into adding a table action, one that requires forms for
                gathering data, you can walk through <reference internal="True" refuri="table_actions#tutorials-table-actions"><inline classes="std std-ref">Tutorial: Adding a complex action to a table</inline></reference> tutorial.</paragraph>
        </section>
        <section ids="conclusion" names="conclusion">
            <title>Conclusion</title>
            <paragraph>What you’ve learned here is the fundamentals of how to write interfaces for
                your own project based on the components horizon provides.</paragraph>
            <paragraph>If you have feedback on how this tutorial could be improved, please feel free
                to submit a bug against <literal>Horizon</literal> in
                <reference name="launchpad" refuri="https://bugs.launchpad.net/horizon">launchpad</reference>.</paragraph>
        </section>
    </section>
</document>
