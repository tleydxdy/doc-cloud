<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/cinder/doc/source/cli/cli-cinder-scheduling.rst">
    <section ids="manage-block-storage-scheduling" names="manage\ block\ storage\ scheduling">
        <title>Manage Block Storage scheduling</title>
        <paragraph>As an administrative user, you have some control over which volume
            back end your volumes reside on. You can specify affinity or
            anti-affinity between two volumes. Affinity between volumes means
            that they are stored on the same back end, whereas anti-affinity
            means that they are stored on different back ends.</paragraph>
        <paragraph>For information on how to set up multiple back ends for Cinder,
            refer to <reference internal="True" refuri="../admin/blockstorage-multi-backend#multi-backend"><inline classes="std std-ref">Configure multiple-storage back ends</inline></reference>.</paragraph>
        <section ids="example-usages" names="example\ usages">
            <title>Example Usages</title>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>Create a new volume on the same back end as Volume_A:</paragraph>
                    <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --hint same_host=Volume_A-UUID \
  --size SIZE VOLUME_NAME</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Create a new volume on a different back end than Volume_A:</paragraph>
                    <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --hint different_host=Volume_A-UUID \
  --size SIZE VOLUME_NAME</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Create a new volume on the same back end as Volume_A and Volume_B:</paragraph>
                    <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --hint same_host=Volume_A-UUID \
  --hint same_host=Volume_B-UUID --size SIZE VOLUME_NAME</literal_block>
                    <paragraph>Or:</paragraph>
                    <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --hint same_host="[Volume_A-UUID, \
  Volume_B-UUID]" --size SIZE VOLUME_NAME</literal_block>
                </list_item>
                <list_item>
                    <paragraph>Create a new volume on a different back end than both Volume_A and
                        Volume_B:</paragraph>
                    <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --hint different_host=Volume_A-UUID \
  --hint different_host=Volume_B-UUID --size SIZE VOLUME_NAME</literal_block>
                    <paragraph>Or:</paragraph>
                    <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --hint different_host="[Volume_A-UUID, \
  Volume_B-UUID]" --size SIZE VOLUME_NAME</literal_block>
                </list_item>
            </enumerated_list>
        </section>
    </section>
</document>
