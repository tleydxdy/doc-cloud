<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/cinder/doc/source/admin/ts-duplicate-3par-host.rst">
    <section ids="duplicate-3par-host" names="duplicate\ 3par\ host">
        <title>Duplicate 3PAR host</title>
        <section ids="problem" names="problem">
            <title>Problem</title>
            <paragraph>This error may be caused by a volume being exported outside of OpenStack
                using a host name different from the system name that OpenStack expects.
                This error could be displayed with the <reference internal="True" refuri="../common/glossary#term-iscsi-qualified-name-iqn"><inline classes="xref std std-term">IQN</inline></reference> if the host was exported using iSCSI:</paragraph>
            <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">Duplicate3PARHost: 3PAR Host already exists: Host wwn 50014380242B9750 \
already used by host cld4b5ubuntuW(id = 68. The hostname must be called\
'cld4b5ubuntu'.</literal_block>
        </section>
        <section ids="solution" names="solution">
            <title>Solution</title>
            <paragraph>Change the 3PAR host name to match the one that OpenStack expects. The
                3PAR host constructed by the driver uses just the local host name, not
                the fully qualified domain name (FQDN) of the compute host. For example,
                if the FQDN was <emphasis>myhost.example.com</emphasis>, just <emphasis>myhost</emphasis> would be used as the
                3PAR host name. IP addresses are not allowed as host names on the 3PAR
                storage server.</paragraph>
        </section>
    </section>
</document>
