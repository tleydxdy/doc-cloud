<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/cinder/doc/source/admin/blockstorage-multi-backend.rst">
    <target refid="multi-backend"></target>
    <section ids="configure-multiple-storage-back-ends multi-backend" names="configure\ multiple-storage\ back\ ends multi_backend">
        <title>Configure multiple-storage back ends</title>
        <paragraph>When you configure multiple-storage back ends, you can create several
            back-end storage solutions that serve the same OpenStack Compute
            configuration and one <literal>cinder-volume</literal> is launched for each back-end
            storage or back-end storage pool.</paragraph>
        <paragraph>In a multiple-storage back-end configuration, each back end has a name
            (<literal>volume_backend_name</literal>). Several back ends can have the same name.
            In that case, the scheduler properly decides which back end the volume
            has to be created in.</paragraph>
        <paragraph>The name of the back end is declared as an extra-specification of a
            volume type (such as, <literal>volume_backend_name=LVM</literal>). When a volume
            is created, the scheduler chooses an appropriate back end to handle the
            request, according to the volume type specified by the user.</paragraph>
        <section ids="enable-multiple-storage-back-ends" names="enable\ multiple-storage\ back\ ends">
            <title>Enable multiple-storage back ends</title>
            <paragraph>To enable a multiple-storage back ends, you must set the
                <title_reference>enabled_backends</title_reference> flag in the <literal>cinder.conf</literal> file.
                This flag defines the names (separated by a comma) of the configuration
                groups for the different back ends: one name is associated to one
                configuration group for a back end (such as, <literal>[lvmdriver-1]</literal>).</paragraph>
            <note>
                <paragraph>The configuration group name is not related to the <literal>volume_backend_name</literal>.</paragraph>
            </note>
            <note>
                <paragraph>After setting the <literal>enabled_backends</literal> flag on an existing cinder
                    service, and restarting the Block Storage services, the original <literal>host</literal>
                    service is replaced with a new host service. The new service appears
                    with a name like <literal>host@backend</literal>. Use:</paragraph>
                <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ cinder-manage volume update_host --currenthost CURRENTHOST --newhost CURRENTHOST@BACKEND</literal_block>
                <paragraph>to convert current block devices to the new host name.</paragraph>
            </note>
            <paragraph>The options for a configuration group must be defined in the group
                (or default options are used). All the standard Block Storage
                configuration options (<literal>volume_group</literal>, <literal>volume_driver</literal>, and so on)
                might be used in a configuration group. Configuration values in
                the <literal>[DEFAULT]</literal> configuration group are not used.</paragraph>
            <paragraph>These examples show three back ends:</paragraph>
            <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">enabled_backends=lvmdriver-1,lvmdriver-2,lvmdriver-3
[lvmdriver-1]
volume_group=cinder-volumes-1
volume_driver=cinder.volume.drivers.lvm.LVMVolumeDriver
volume_backend_name=LVM
[lvmdriver-2]
volume_group=cinder-volumes-2
volume_driver=cinder.volume.drivers.lvm.LVMVolumeDriver
volume_backend_name=LVM
[lvmdriver-3]
volume_group=cinder-volumes-3
volume_driver=cinder.volume.drivers.lvm.LVMVolumeDriver
volume_backend_name=LVM_b</literal_block>
            <paragraph>In this configuration, <literal>lvmdriver-1</literal> and <literal>lvmdriver-2</literal> have the same
                <literal>volume_backend_name</literal>. If a volume creation requests the <literal>LVM</literal>
                back end name, the scheduler uses the capacity filter scheduler to choose
                the most suitable driver, which is either <literal>lvmdriver-1</literal> or <literal>lvmdriver-2</literal>.
                The capacity filter scheduler is enabled by default. The next section
                provides more information. In addition, this example presents a
                <literal>lvmdriver-3</literal> back end.</paragraph>
            <note>
                <paragraph>For Fiber Channel drivers that support multipath, the configuration group
                    requires the <literal>use_multipath_for_image_xfer=true</literal> option. In
                    the example below, you can see details for HPE 3PAR and EMC Fiber
                    Channel drivers.</paragraph>
            </note>
            <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[3par]
use_multipath_for_image_xfer = true
volume_driver = cinder.volume.drivers.hpe.hpe_3par_fc.HPE3PARFCDriver
volume_backend_name = 3parfc

[emc]
use_multipath_for_image_xfer = true
volume_driver = cinder.volume.drivers.emc.emc_smis_fc.EMCSMISFCDriver
volume_backend_name = emcfc</literal_block>
        </section>
        <section ids="configure-block-storage-scheduler-multi-back-end" names="configure\ block\ storage\ scheduler\ multi\ back\ end">
            <title>Configure Block Storage scheduler multi back end</title>
            <paragraph>You must enable the <title_reference>filter_scheduler</title_reference> option to use
                multiple-storage back ends. The filter scheduler:</paragraph>
            <enumerated_list enumtype="arabic" prefix="" suffix=".">
                <list_item>
                    <paragraph>Filters the available back ends. By default, <literal>AvailabilityZoneFilter</literal>,
                        <literal>CapacityFilter</literal> and <literal>CapabilitiesFilter</literal> are enabled.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Weights the previously filtered back ends. By default, the
                        <title_reference>CapacityWeigher</title_reference> option is enabled. When this option is
                        enabled, the filter scheduler assigns the highest weight to back
                        ends with the most available capacity.</paragraph>
                </list_item>
            </enumerated_list>
            <paragraph>The scheduler uses filters and weights to pick the best back end to
                handle the request. The scheduler uses volume types to explicitly create
                volumes on specific back ends. For more information about filter and weighing,
                see <reference internal="True" refuri="blockstorage-driver-filter-weighing#filter-weigh-scheduler"><inline classes="std std-ref">Configure and use driver filter and weighing for scheduler</inline></reference>.</paragraph>
        </section>
        <section ids="volume-type" names="volume\ type">
            <title>Volume type</title>
            <paragraph>Before using it, a volume type has to be declared to Block Storage.
                This can be done by the following command:</paragraph>
            <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack --os-username admin --os-tenant-name admin volume type create lvm</literal_block>
            <paragraph>Then, an extra-specification has to be created to link the volume
                type to a back end name. Run this command:</paragraph>
            <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack --os-username admin --os-tenant-name admin volume type set lvm \
  --property volume_backend_name=LVM_iSCSI</literal_block>
            <paragraph>This example creates a <literal>lvm</literal> volume type with
                <literal>volume_backend_name=LVM_iSCSI</literal> as extra-specifications.</paragraph>
            <paragraph>Create another volume type:</paragraph>
            <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack --os-username admin --os-tenant-name admin volume type create lvm_gold

$ openstack --os-username admin --os-tenant-name admin volume type set lvm_gold \
  --property volume_backend_name=LVM_iSCSI_b</literal_block>
            <paragraph>This second volume type is named <literal>lvm_gold</literal> and has <literal>LVM_iSCSI_b</literal> as
                back end name.</paragraph>
            <note>
                <paragraph>To list the extra-specifications, use this command:</paragraph>
                <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack --os-username admin --os-tenant-name admin volume type list --long</literal_block>
            </note>
            <note>
                <paragraph>If a volume type points to a <literal>volume_backend_name</literal> that does not
                    exist in the Block Storage configuration, the <literal>filter_scheduler</literal>
                    returns an error that it cannot find a valid host with the suitable
                    back end.</paragraph>
            </note>
        </section>
        <section ids="usage" names="usage">
            <title>Usage</title>
            <paragraph>When you create a volume, you must specify the volume type.
                The extra-specifications of the volume type are used to determine which
                back end has to be used.</paragraph>
            <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --size 1 --type lvm test_multi_backend</literal_block>
            <paragraph>Considering the <literal>cinder.conf</literal> described previously, the scheduler
                creates this volume on <literal>lvmdriver-1</literal> or <literal>lvmdriver-2</literal>.</paragraph>
            <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ openstack volume create --size 1 --type lvm_gold test_multi_backend</literal_block>
            <paragraph>This second volume is created on <literal>lvmdriver-3</literal>.</paragraph>
        </section>
    </section>
</document>
