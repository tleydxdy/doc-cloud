<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/neutron/doc/source/admin/config-ovsfwdriver.rst">
    <target refid="config-ovsfwdriver"></target>
    <section ids="native-open-vswitch-firewall-driver config-ovsfwdriver" names="native\ open\ vswitch\ firewall\ driver config-ovsfwdriver">
        <title>Native Open vSwitch firewall driver</title>
        <note>
            <paragraph>Experimental feature or incomplete documentation.</paragraph>
        </note>
        <paragraph>Historically, Open vSwitch (OVS) could not interact directly with <emphasis>iptables</emphasis>
            to implement security groups. Thus, the OVS agent and Compute service use
            a Linux bridge between each instance (VM) and the OVS integration bridge
            <literal>br-int</literal> to implement security groups. The Linux bridge device contains
            the <emphasis>iptables</emphasis> rules pertaining to the instance. In general, additional
            components between instances and physical network infrastructure cause
            scalability and performance problems. To alleviate such problems, the OVS
            agent includes an optional firewall driver that natively implements security
            groups as flows in OVS rather than the Linux bridge device and <emphasis>iptables</emphasis>.
            This increases scalability and performance.</paragraph>
        <section ids="configuring-heterogeneous-firewall-drivers" names="configuring\ heterogeneous\ firewall\ drivers">
            <title>Configuring heterogeneous firewall drivers</title>
            <paragraph>L2 agents can be configured to use differing firewall drivers. There is no
                requirement that they all be the same. If an agent lacks a firewall driver
                configuration, it will default to what is configured on its server. This also
                means there is no requirement that the server has any firewall driver
                configured at all, as long as the agents are configured correctly.</paragraph>
        </section>
        <section ids="prerequisites" names="prerequisites">
            <title>Prerequisites</title>
            <paragraph>The native OVS firewall implementation requires kernel and user space support
                for <emphasis>conntrack</emphasis>, thus requiring minimum versions of the Linux kernel and
                Open vSwitch. All cases require Open vSwitch version 2.5 or newer.</paragraph>
            <bullet_list bullet="*">
                <list_item>
                    <paragraph>Kernel version 4.3 or newer includes <emphasis>conntrack</emphasis> support.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>Kernel version 3.3, but less than 4.3, does not include <emphasis>conntrack</emphasis>
                        support and requires building the OVS modules.</paragraph>
                </list_item>
            </bullet_list>
        </section>
        <section ids="enable-the-native-ovs-firewall-driver" names="enable\ the\ native\ ovs\ firewall\ driver">
            <title>Enable the native OVS firewall driver</title>
            <bullet_list bullet="*">
                <list_item>
                    <paragraph>On nodes running the Open vSwitch agent, edit the
                        <literal>openvswitch_agent.ini</literal> file and enable the firewall driver.</paragraph>
                    <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[securitygroup]
firewall_driver = openvswitch</literal_block>
                </list_item>
            </bullet_list>
            <paragraph>For more information, see the
                <reference internal="True" refuri="../contributor/internals/openvswitch_firewall"><inline classes="doc">Open vSwitch Firewall Driver</inline></reference>
                and the <reference name="video" refuri="https://www.youtube.com/watch?v=SOHeZ3g9yxM">video</reference><target ids="video" names="video" refuri="https://www.youtube.com/watch?v=SOHeZ3g9yxM"></target>.</paragraph>
        </section>
    </section>
</document>
