<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.13.1 -->
<document source="/home/fbaumanis/openstack/soc8_test/openstack_repo/neutron/doc/source/admin/config-dvr-ha-snat.rst">
    <target refid="config-dvr-snat-ha-ovs"></target>
    <section ids="distributed-virtual-routing-with-vrrp config-dvr-snat-ha-ovs" names="distributed\ virtual\ routing\ with\ vrrp config-dvr-snat-ha-ovs">
        <title>Distributed Virtual Routing with VRRP</title>
        <paragraph><reference internal="True" refuri="deploy-ovs-ha-dvr#deploy-ovs-ha-dvr"><inline classes="std std-ref">Open vSwitch: High availability using DVR</inline></reference> supports augmentation
            using Virtual Router Redundancy Protocol (VRRP). Using this configuration,
            virtual routers support both the <literal>--distributed</literal> and <literal>--ha</literal> options.</paragraph>
        <paragraph>Similar to legacy HA routers, DVR/SNAT HA routers provide a quick fail over of
            the SNAT service to a backup DVR/SNAT router on an l3-agent running on a
            different node.</paragraph>
        <paragraph>SNAT high availability is implemented in a manner similar to the
            <reference internal="True" refuri="deploy-lb-ha-vrrp#deploy-lb-ha-vrrp"><inline classes="std std-ref">Linux bridge: High availability using VRRP</inline></reference> and <reference internal="True" refuri="deploy-ovs-ha-vrrp#deploy-ovs-ha-vrrp"><inline classes="std std-ref">Open vSwitch: High availability using VRRP</inline></reference> examples where
            <literal>keepalived</literal> uses VRRP to provide quick failover of SNAT services.</paragraph>
        <paragraph>During normal operation, the master router periodically transmits <emphasis>heartbeat</emphasis>
            packets over a hidden project network that connects all HA routers for a
            particular project.</paragraph>
        <paragraph>If the DVR/SNAT backup router stops receiving these packets, it assumes failure
            of the master DVR/SNAT router and promotes itself to master router by
            configuring IP addresses on the interfaces in the <literal>snat</literal> namespace. In
            environments with more than one backup router, the rules of VRRP are followed
            to select a new master router.</paragraph>
        <warning>
            <paragraph>There is a known bug with <literal>keepalived</literal> v1.2.15 and earlier which can
                cause packet loss when <literal>max_l3_agents_per_router</literal> is set to 3 or more.
                Therefore, we recommend that you upgrade to <literal>keepalived</literal> v1.2.16
                or greater when using this feature.</paragraph>
        </warning>
        <note>
            <paragraph>Experimental feature or incomplete documentation.</paragraph>
        </note>
        <section ids="configuration-example" names="configuration\ example">
            <title>Configuration example</title>
            <paragraph>The basic deployment model consists of one controller node, two or more network
                nodes, and multiple computes nodes.</paragraph>
            <section ids="controller-node-configuration" names="controller\ node\ configuration">
                <title>Controller node configuration</title>
                <enumerated_list enumtype="arabic" prefix="" suffix=".">
                    <list_item>
                        <paragraph>Add the following to <literal>/etc/neutron/neutron.conf</literal>:</paragraph>
                        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[DEFAULT]
core_plugin = ml2
service_plugins = router
allow_overlapping_ips = True
router_distributed = True
l3_ha = True
l3_ha_net_cidr = 169.254.192.0/18
max_l3_agents_per_router = 3</literal_block>
                        <paragraph>When the <literal>router_distributed = True</literal> flag is configured, routers created
                            by all users are distributed. Without it, only privileged users can create
                            distributed routers by using <literal>--distributed True</literal>.</paragraph>
                        <paragraph>Similarly, when the <literal>l3_ha = True</literal> flag is configured, routers created
                            by all users default to HA.</paragraph>
                        <paragraph>It follows that with these two flags set to <literal>True</literal> in the configuration
                            file, routers created by all users will default to distributed HA routers
                            (DVR HA).</paragraph>
                        <paragraph>The same can explicitly be accomplished by a user with administrative
                            credentials setting the flags in the <literal_strong classes="command">neutron router-create</literal_strong>
                            command:</paragraph>
                        <literal_block highlight_args="{}" language="console" linenos="False" xml:space="preserve">$ neutron router-create name-of-router --distributed=True --ha=True</literal_block>
                        <note>
                            <paragraph>The <emphasis>max_l3_agents_per_router</emphasis> determine the number of backup
                                DVR/SNAT routers which  will be instantiated.</paragraph>
                        </note>
                    </list_item>
                    <list_item>
                        <paragraph>Add the following to <literal>/etc/neutron/plugins/ml2/ml2_conf.ini</literal>:</paragraph>
                        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[ml2]
type_drivers = flat,vxlan
tenant_network_types = vxlan
mechanism_drivers = openvswitch,l2population
extension_drivers = port_security

[ml2_type_flat]
flat_networks = external

[ml2_type_vxlan]
vni_ranges = MIN_VXLAN_ID:MAX_VXLAN_ID</literal_block>
                        <paragraph>Replace <literal>MIN_VXLAN_ID</literal> and <literal>MAX_VXLAN_ID</literal> with  VXLAN ID minimum and
                            maximum values suitable for your environment.</paragraph>
                        <note>
                            <paragraph>The first value in the <literal>tenant_network_types</literal> option becomes the
                                default project network type when a regular user creates a network.</paragraph>
                        </note>
                    </list_item>
                </enumerated_list>
            </section>
            <section ids="network-nodes" names="network\ nodes">
                <title>Network nodes</title>
                <enumerated_list enumtype="arabic" prefix="" suffix=".">
                    <list_item>
                        <paragraph>Configure the Open vSwitch agent. Add the following to
                            <literal>/etc/neutron/plugins/ml2/ml2_conf.ini</literal>:</paragraph>
                        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[ovs]
local_ip = TUNNEL_INTERFACE_IP_ADDRESS
bridge_mappings = external:br-ex

[agent]
enable_distributed_routing = True
tunnel_types = vxlan
l2_population = True</literal_block>
                        <paragraph>Replace <literal>TUNNEL_INTERFACE_IP_ADDRESS</literal> with the IP address of the interface
                            that handles VXLAN project networks.</paragraph>
                    </list_item>
                    <list_item>
                        <paragraph>Configure the L3 agent. Add the following to <literal>/etc/neutron/l3_agent.ini</literal>:</paragraph>
                        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[DEFAULT]
ha_vrrp_auth_password = password
interface_driver = openvswitch
external_network_bridge =
agent_mode = dvr_snat</literal_block>
                        <note>
                            <paragraph>The <literal>external_network_bridge</literal> option intentionally contains
                                no value.</paragraph>
                        </note>
                    </list_item>
                </enumerated_list>
            </section>
            <section ids="compute-nodes" names="compute\ nodes">
                <title>Compute nodes</title>
                <enumerated_list enumtype="arabic" prefix="" suffix=".">
                    <list_item>
                        <paragraph>Configure the Open vSwitch agent. Add the following to
                            <literal>/etc/neutron/plugins/ml2/ml2_conf.ini</literal>:</paragraph>
                        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[ovs]
local_ip = TUNNEL_INTERFACE_IP_ADDRESS
bridge_mappings = external:br-ex

[agent]
enable_distributed_routing = True
tunnel_types = vxlan
l2_population = True

[securitygroup]
firewall_driver = neutron.agent.linux.iptables_firewall.OVSHybridIptablesFirewallDriver</literal_block>
                    </list_item>
                    <list_item>
                        <paragraph>Configure the L3 agent. Add the following to <literal>/etc/neutron/l3_agent.ini</literal>:</paragraph>
                        <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">[DEFAULT]
interface_driver = openvswitch
external_network_bridge =
agent_mode = dvr</literal_block>
                        <paragraph>Replace <literal>TUNNEL_INTERFACE_IP_ADDRESS</literal> with the IP address of the interface
                            that handles VXLAN project networks.</paragraph>
                    </list_item>
                </enumerated_list>
            </section>
            <section ids="keepalived-vrrp-health-check" names="keepalived\ vrrp\ health\ check">
                <title>Keepalived VRRP health check</title>
                <paragraph>The health of your <literal>keepalived</literal> instances can be automatically monitored via
                    a bash script that verifies connectivity to all available and configured
                    gateway addresses. In the event that connectivity is lost, the master router
                    is rescheduled to another node.</paragraph>
                <paragraph>If all routers lose connectivity simultaneously, the process of selecting a
                    new master router will be repeated in a round-robin fashion until one or more
                    routers have their connectivity restored.</paragraph>
                <paragraph>To enable this feature, edit the <literal>l3_agent.ini</literal> file:</paragraph>
                <literal_block highlight_args="{}" language="ini" linenos="False" xml:space="preserve">ha_vrrp_health_check_interval = 30</literal_block>
                <paragraph>Where <literal>ha_vrrp_health_check_interval</literal> indicates how often in seconds the
                    health check should run. The default value is <literal>0</literal>, which indicates that the
                    check should not run at all.</paragraph>
            </section>
        </section>
        <section ids="known-limitations" names="known\ limitations">
            <title>Known limitations</title>
            <bullet_list bullet="*">
                <list_item>
                    <paragraph>Migrating a router from distributed only, HA only, or legacy to distributed
                        HA is not supported at this time. The router must be created as distributed
                        HA.
                        The reverse direction is also not supported. You cannot reconfigure a
                        distributed HA router to be only distributed, only HA, or legacy.</paragraph>
                </list_item>
                <list_item>
                    <paragraph>There are certain scenarios where l2pop and distributed HA routers do not
                        interact in an expected manner. These situations are the same that affect HA
                        only routers and l2pop.</paragraph>
                </list_item>
            </bullet_list>
        </section>
    </section>
</document>
